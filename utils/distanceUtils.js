// Utility function to calculate distance between patient and hospitals.
// We'll replace this with actual Google Maps API or something else later.

const calculateDistance = (loc1, loc2) => {
    return Math.random() * 100;
};

module.exports = { calculateDistance };